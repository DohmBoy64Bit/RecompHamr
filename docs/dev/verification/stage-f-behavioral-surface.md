# Stage F Behavioral Surface Inventory

This accepted inventory covers the two verified Legacy RecompHamr tool capabilities and the new application-configured tool boundary. It does not authorize commands, skills, MCP, diagnostics, arbitrary remotes, general browsing, or TUI changes.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| TOOLSET-01 | Application composition configures an immutable local tool set; every model round exposes the accepted six schemas in stable order; execution and inline status share the same registry; unknown names fail closed without Stage G/H fallback. New ownership surface preserving Stage A/C/E lifecycle. | `internal/app`, `internal/agent`, `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence applies to configured cache identity; migration is not applicable because unsafe Legacy globals/cache contents are not imported | Complete composition, request, registry, executor, architecture, and deletion-boundary tests at 100%; D-018; Stage F packet and architecture docs | Exact build `d7286be` advertised six tools, executed both Stage F tools exactly once, retained four ordered baseline results, suppressed a cancelled fifth result/side effect, recovered, and restored the shell | verified |
| REPOMIX-01 | `repomixr` shallow-clones one strict public GitHub owner/repository branch or tag, deterministically selects bounded UTF-8 source, applies requested transforms, safely packs XML plus optional instruction, atomically persists it beneath the protected project cache, and returns a bounded summary. Improved Legacy parity. | `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; migration is not applicable because existing unsafe cache entries are refused/replaced only through the new contract | Complete schema/URL/process/filter/XML/CDATA/instruction/atomic/permission/ACL/link/reparse/bounds/error suite at 100%; `docs/user/tools.md`; D-018; Stage F packet | Exact build `d7286be` cloned `octocat/Hello-World` branch `master`, produced the asserted packed XML, returned one paired result, and displayed the expected status; reviewed screenshot retained outside the repository | verified |
| REFERENCE-01 | `recomp_reference` fetches one public HTTP(S) page, revalidates every redirect/dial destination, bounds time/body/HTML work, reduces readable text, atomically persists a source/timestamp-prefixed file beneath the protected project cache, and reuses it for 24 hours. Improved Legacy parity. | `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; migration is not applicable because cache naming/security is intentionally strengthened | Complete schema/URL/redirect/DNS/HTTP/content-type/HTML/body/cache/clock/atomic/permission/ACL/link/reparse/bounds/error suite at 100%; `docs/user/tools.md`; D-018; Stage F packet | Exact build `d7286be` fetched `https://example.com/`, produced the asserted hashed cache file, returned one paired result, and restored the shell; deterministic tests independently prove private/reserved-network refusal | verified |

The Legacy Bash implementation is intentionally changed to the already accepted native `powershell` contract. MCP tools remain Stage H, and skill/command surfaces remain Stage G.

Stage F has no blocked or unverified row. Source/Legacy traceability, focused and canonical automation, exact-build real-model reports, reviewed screenshots, protected Windows cache ACLs, documentation checks, and dual-platform CI agree.
