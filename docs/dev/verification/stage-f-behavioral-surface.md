# Stage F Behavioral Surface Inventory

This active inventory covers the two verified Legacy RecompHamr tool capabilities and the new application-configured tool boundary. It does not authorize commands, skills, MCP, diagnostics, arbitrary remotes, general browsing, or TUI changes.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| TOOLSET-01 | Application composition configures an immutable local tool set; every model round exposes the accepted six schemas in stable order; execution and inline status share the same registry; unknown names fail closed without Stage G/H fallback. New ownership surface preserving Stage A/C/E lifecycle. | `internal/app`, `internal/agent`, `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence applies to configured cache identity; migration is not applicable because unsafe Legacy globals/cache contents are not imported | Planned composition, request, registry, executor, architecture, and deletion-boundary tests; Stage F packet and architecture docs | Exact-build six-tool session and retained four-tool/cancellation scenarios required | unverified |
| REPOMIX-01 | `repomixr` shallow-clones one strict public GitHub owner/repository branch or tag, deterministically selects bounded UTF-8 source, applies requested transforms, safely packs XML plus optional instruction, atomically persists it beneath the protected project cache, and returns a bounded summary. Improved Legacy parity. | `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; migration is not applicable because existing unsafe cache entries are refused/replaced only through the new contract | Planned schema/URL/process/filter/XML/CDATA/instruction/atomic/permission/ACL/link/reparse/bounds/error tests; `docs/user/tools.md`; Stage F packet | Exact-build model-requested clone/pack/readback against a small public fixture required | unverified |
| REFERENCE-01 | `recomp_reference` fetches one public HTTP(S) page, revalidates every redirect/dial destination, bounds time/body/HTML work, reduces readable text, atomically persists a source/timestamp-prefixed file beneath the protected project cache, and reuses it for 24 hours. Improved Legacy parity. | `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; migration is not applicable because cache naming/security is intentionally strengthened | Planned schema/URL/redirect/DNS/HTTP/content-type/HTML/body/cache/clock/atomic/permission/ACL/link/reparse/bounds/error tests; `docs/user/tools.md`; Stage F packet | Exact-build controlled public or local-policy-safe fixture fetch/readback required; private-network refusal must be independently demonstrated | unverified |

The Legacy Bash implementation is intentionally changed to the already accepted native `powershell` contract. MCP tools remain Stage H, and skill/command surfaces remain Stage G.
