# Stage C Behavioral Surface Inventory

This is the accepted inventory for separation-of-concerns ownership moves. It supplements the accepted Stage A inventory rather than replacing its evidence. Every in-scope row is verified and every retained Stage A contract remains verified.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| APP-01 | Process startup and composition remain outside presentation with equivalent behavior. Stage C ownership move. | `cmd/recomphamr`, `internal/app` | success, failure, environment, cleanup, security, lifecycle, compatibility; malformed input, timeout, Unicode, persistence, migration, and concurrency are not applicable because this surface only composes already-tested services | command/application tests; current architecture and Stage C work packet | Accepted exact-build startup, resize, exit, and restored shell | verified |
| AGENT-01 | A stable turn identity spans context packing, every model round, accounting, cancellation, and stale-event rejection while presentation receives ordered immutable facts. Stage C replacement boundary preserving `TUI-02`. | `internal/agent`, `internal/app`, TUI adapter | success, failure, malformed input, boundary, cancellation, cleanup, compatibility, concurrency, security; timeout is exercised through stream/tool dependencies; Unicode applies to content preservation; persistence and migration are not applicable because turn state is non-durable; platform applies at runtime | `TestRuntimeLifecycleAndObserver`, `TestRuntimeStreamObservation`, `TestRuntimeCancelTurnDropsOnlyRunningToolGoal`, `TestApplyDeliveryValidatesAndReducesOpaqueEvents`, `TestStreamEventStateMachine`, and retained queue/render tests; Stage C work packet and architecture docs | Passed exact-build Gemma scenario report and reviewed `tools-complete.png`, `powershell-running.png`, and `recovered.png` under `E:\ReProject\StageA-Acceptance\StageC-Agent-Slice2`; recovery contains no cancelled-command reissue | verified |
| AGENT-02 | Tool calls execute sequentially in emission order, every accepted call receives one paired result before the next model round, cancellation cleans up active work, and existing failure/runaway/empty/verification policy remains unchanged. Stage C ownership move preserving `TUI-02`, `TOOL-01`, and `TOOL-02`. | `internal/agent`, `internal/tools` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, compatibility, concurrency, security; Unicode applies through tool arguments/results; persistence and migration are not applicable because the loop is non-durable | `TestSequentialToolPairingAndProductionExecutor`, `TestToolCancellationAndStaleCleanup`, `TestRuntimeCancelTurnDropsOnlyRunningToolGoal`, `TestToolFailureAndRunawayPolicy`, `TestClosePolicy`, `TestRuntimeToolAndCloseObservation`, and adapter tests; Stage C work packet and architecture docs | Passed exact-build report proves exactly five starts (four ordered fixture calls plus the cancelled PowerShell), exactly four accepted results, absent cancelled side effect, recovery without reissue, cleanup, and restored shell | verified |
| TUI-04 | Input is translated into agent intents and ordered agent events reproduce the accepted phase, status, transcript, queue, cancellation, and footer behavior without presentation owning model/tool work. Stage C adapter surface preserving `TUI-01` through `TUI-03`. | `internal/tui`, `internal/app` | success, failure, boundary, cancellation, cleanup, platform, Unicode, compatibility, concurrency, security; malformed agent events are covered at the typed boundary; timeout is represented by agent diagnostics; persistence and migration remain covered by retained Stage A rows | `TestStreamEventStateMachine`, `TestQueueAutoFiresAfterNaturalFinish`, `TestQueueRestoredOnCtrlC`, `TestToolResultAdapterEmitsPolicyLogs`, retained render/input/resize suites, and architecture checks; TUI instructions and architecture docs | Reviewed exact-build 100×30 screenshots show frozen composition during completed tools, running cancellation target, and clean recovery; report proves input ordering, cancellation state, clean exit, and restored shell | verified |
| SESSION-01 | Configuration reload, active-profile persistence, process-only URL override preservation, and concrete model-client replacement are atomic outside presentation while `/models` behavior remains equivalent. Stage C ownership move preserving `CFG-01`, `LLM-01`, and `TUI-03`. | `internal/session`, `internal/config`, `internal/app` | success, failure, malformed input, boundary, persistence, migration, compatibility, concurrency, security; platform applies through config persistence; cancellation and timeout are not applicable because reload/activation are bounded synchronous file operations; Unicode applies to profile metadata | `TestRuntimeSnapshotActivationAndReload`, `TestRuntimeHistoryDelegationAndActivateRollback`, app composition tests, retained config security/platform suites, slash/popover/render tests, architecture enforcement, decision D-014, and Slice 3 packet | User-confirmed Windows Terminal model list/switch, real Gemma response, persisted restart selection, and unchanged representative rendering on 2026-07-15 | verified |
| SESSION-02 | Backend reachability and authenticated probe execution occur outside presentation, preserve timeouts and stale-profile rejection, and expose only non-secret typed results. Stage C ownership move preserving `LLM-01` and `TUI-03`. | `internal/session`, `internal/provider`, `internal/llm` | success, failure, boundary, cancellation, timeout, cleanup, platform, compatibility, concurrency, security; malformed transport results are covered by retained transport tests; Unicode, persistence, and migration are not applicable to ephemeral probe results | `TestRuntimeOverrideChatAndCapturedWork`, retained LLM/provider transport suites, `TestProbeSuccessFailureAndBackendCommands`, `TestUpdateTypedMessagesAndClosedOutcomes`, architecture enforcement, decision D-014, and Slice 3 packet | User-confirmed reachable startup, switched-profile connectivity, and real `google/gemma-4-12b-qat` response on 2026-07-15 | verified |
| SESSION-03 | Prompt history load, append, bounded trim, recovery, secure storage, concurrent access, recall, and clear are owned outside presentation with the existing on-disk format and UI behavior. Stage C ownership move preserving `PERSIST-01`, `SEC-01`, and `TUI-01`. | `internal/session`, `internal/config` security boundary | success, failure, malformed input, boundary, cleanup, platform, Unicode, persistence, migration, compatibility, concurrency, security; cancellation and timeout are not applicable because operations are synchronous bounded local-file actions | `TestHistoryRoundTripBoundsAndClear`, `TestHistoryTrimMalformedAndOversizeRecovery`, `TestHistoryConcurrentAppendsKeepBoth`, `TestHistoryFailureAndScannerBoundaries`, `TestNewLoadsInjectedPromptHistory`, retained prompt-history interaction tests, architecture enforcement, and Slice 3 packet | User-confirmed restart recall and absence after `/clear` on 2026-07-15 | verified |
| TUI-05 | Presentation renders immutable session facts and translates `/models`, `/clear`, startup, and history interactions into typed intents without owning config, client, probe, or filesystem lifecycle. Stage C replacement boundary preserving `TUI-01` and `TUI-03`. | `internal/tui`, `internal/app` | success, failure, boundary, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; malformed typed results and timeout diagnostics apply; migration is covered by session/config owners; cancellation is not applicable because profile changes remain prohibited during an active turn | `TestSlashCommandsModelSwitchAndHelp`, `TestHandleEnterSelectionAndSlashCommit`, `TestProbeSuccessFailureAndBackendCommands`, `TestUpdateTypedMessagesAndClosedOutcomes`, `TestNewLoadsInjectedPromptHistory`, retained render/input/resize suites, architecture enforcement, TUI instructions, architecture docs, and Slice 3 packet | User-confirmed startup, model list/switch, persistence, history, 120×36/80×24/50×16 visual inspection, clean exit, and restored PowerShell on 2026-07-15; no screenshot artifacts supplied | verified |
| APP-02 | The application controller translates each frontend intent/completion into exactly one agent/session action, emits ordered immutable presentation facts, and rejects duplicate, malformed, or stale work without leaking backend capabilities. Stage C closure boundary. | `internal/app`, `internal/frontend` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; migration is not applicable because the contract preserves the current on-disk/runtime formats | Controller contract tests cover every intent, ordered model/tool transitions, retries, cancellation, malformed/duplicate/stale completions, profile/probe/history lifecycle, copy safety, and secrecy; Slice 4 packet and D-015 | Exact-build Gemma stream/tool cancellation and frontend checklist reports under `E:\ReProject\StageA-Acceptance\StageC-Slice4-Final\72e6b43215cc14f91eb6547de15a7386bc77b927` | verified |
| FRONTEND-01 | Backend-neutral frontend contracts and a thin terminal adapter allow core app/backend behavior to build and test without the concrete TUI while the runnable terminal retains one explicit application-owned wiring edge. Stage C deletion boundary. | `internal/frontend`, `internal/app`, `internal/app/terminal` | success, failure, malformed input, boundary, cleanup, platform, compatibility, concurrency, security; timeout/cancellation/persistence apply through controller work; Unicode applies to display-safe content; migration is not applicable | Accepted architecture, Slice 4 packet, import-graph/deletion checks, package documentation, controller/app/terminal tests, and canonical verification | CI run `29447373333`; exact-build Windows Terminal reports and reviewed 120×36/80×24/50×16 images under the commit-named evidence directory | verified |
| TUI-06 | Presentation owns only Bubble Tea rendering, local editor/popover/queue/transcript/timing state, and input-to-intent translation; it has no backend imports, raw capabilities, or lifecycle calls. Stage C final replacement preserving all accepted TUI rows. | `internal/tui`, `internal/frontend` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; migration is not applicable because visible behavior and formats are frozen | Retained TUI suites, fake-controller intent/event/work adapter tests, architecture enforcement, TUI instructions, and Slice 4 packet | Exact-build smoke, model/tool cancellation, multiline/queue, profile persistence, history recall/clear, three-size rendering, clean exit, and restored shell passed under the commit-named evidence directory | verified |

Stage C has no blocked or unverified row. Source inspection, focused and canonical automation, the deletion-boundary proof, dual-platform CI, documentation audit, exact-build runtime reports, and reviewed screenshots agree. The first exact-build tool scenario attempt was rejected because Gemma emitted an extra fifth fixture tool call; the clean retry passed its strict four-call contract and is retained as `agent-tool-loop-cancel-retry/report.json` rather than concealing model nondeterminism.
