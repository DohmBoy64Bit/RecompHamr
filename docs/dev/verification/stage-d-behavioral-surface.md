# Stage D Behavioral Surface Inventory

This active inventory supplements the accepted Stage A and Stage C inventories. The first Stage D slice is an adapted, security-improved workspace-state foundation; no Legacy command, RE template family, diagnostic bundle, skill, MCP configuration, or tool family is represented as implemented.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| WORKSPACE-01 | One canonical absolute project identity owns `.rehamr` workspace-state resolution without allowing traversal, link/reparse redirection, non-regular inputs, or presentation filesystem access. Adapted Legacy workspace identity with improved security. | planned `internal/workspace`, `internal/app` | success, failure, malformed input, boundary, platform, Unicode, persistence, compatibility, concurrency, security; cancellation and timeout are not applicable to bounded synchronous local metadata/read operations; migration is not applicable because no current workspace-state format exists | Workspace/app tests, architecture enforcement, Stage D packet, workspace user contract, and Go package/API docs required | Exact-build canonical-path and absent-state startup evidence required | unverified |
| MEMORY-01 | Optional `.rehamr/REPHAMR_STATE.md` content is bounded, Unicode-safe, model-facing project context; missing/empty state is silent, changed state refreshes at the defined lifecycle, and contents never enter frontend facts or logs. Adapted Legacy persistent-memory behavior with improved bounds and trust documentation. | planned `internal/workspace`, `internal/app/controller` | success, failure, malformed input, boundary, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; cancellation/timeout are not applicable to bounded synchronous reads; migration applies only to accepting the Legacy filename/content as opaque text | Workspace/controller/agent request tests, secrecy/import checks, Stage D packet, workspace user contract, and prompt trust documentation required | Exact-build absent/present/changed/cleared state behavior with sanitized reports required | unverified |
| APP-03 | Application composition constructs the workspace owner and model-facing prompt source while the TUI continues to receive only existing display-safe frontend contracts. New Stage D ownership surface preserving all Stage C rows. | `internal/app`, planned `internal/workspace`, `internal/app/controller` | success, failure, malformed input, boundary, cleanup, platform, Unicode, persistence, compatibility, concurrency, security; cancellation and timeout apply only to the existing turn work and must remain unchanged; migration is not applicable | App/controller/frontend/TUI tests, architecture/deletion checks, current architecture, D-016 when accepted, and Stage D packet required | Exact-build frozen TUI, real response, cancellation, clean exit, and restored shell required | unverified |

Deferred Legacy evidence is not an active implementation claim: `/init-re` and `/status-re` await Stage G command authorization and a Stage F-safe template subset; `/doctor` must be decomposed among Stage D configuration/environment foundations and later Stage F/G/H owners; project memory is limited in this slice to secure optional loading and does not authorize agent self-maintenance policy.
