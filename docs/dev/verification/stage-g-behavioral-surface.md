# Stage G Behavioral Surface Inventory

This accepted inventory covers Stage G command registries and Agent Skills at implementation commit `6aaeb81ae01f6b0290c63af94791001fff021960`. Stage H MCP behavior and any TUI redesign are excluded.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| COMMAND-01 | One immutable ordered registry defines names, descriptions, arguments, availability, help, completion, and typed dispatch; retained `/clear` and `/models` remain behaviorally identical and unknown commands retain the accepted quiet warning. Retained plus improved Legacy command ownership. | `internal/frontend`, `internal/app/controller`; TUI renders facts and emits intents only | success, failure, malformed input, boundary, cancellation, cleanup, Unicode, compatibility, concurrency, security; timeout/platform/persistence/migration apply only to the invoked backend command | registry/controller/fake-controller/TUI/help/architecture tests; Stage G packet and user command docs | exact-build help, completion, retained command, cancellation, resize, and exit evidence | verified |
| SKILLS-CLIENT-01 | Bounded standards-based discovery scans approved project/user locations, applies deterministic precedence and trust/config filtering, parses metadata, strictly validates RecompHamr skills, records display-safe invalid/skipped/shadowed diagnostics, and exposes no body at catalog time. Standards-based replacement of Legacy loader. | `internal/skills`, composed by `internal/app` | success, failure, malformed input, boundary, platform, Unicode, persistence, migration, compatibility, concurrency, security; cancellation/timeout/cleanup apply to bounded discovery work | discovery/parser/validation/trust/precedence/config/link/reparse/race/bounds/diagnostic tests and user/developer docs | exact-build valid, invalid, shadowed, trusted-project, and catalog evidence; disabled/untrusted behavior is deterministic-test evidence because acceptance configuration deliberately enabled the fixture | verified |
| SKILLS-ACTIVATE-01 | Explicit `/skill <name>` and model-driven activation load one valid skill body on demand, wrap identity/base path/resources, deduplicate activation, preserve active instructions through context packing, and never eagerly read or execute resources. Improved Legacy activation parity. | `internal/skills`, `internal/app/controller`, `internal/agent`/`internal/ctx` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence/migration apply to enable state and Legacy replacement | activation/resource/context/deduplication/stale/cancellation/fake-controller/TUI tests plus user skill docs | exact-build explicit and implicit activation, referenced-resource use, cancellation, stale-result recovery, and no eager execution evidence | verified |
| SKILL-MIGRATION-01 | Every approved Legacy skill is read completely, assigned a coherent trigger boundary, migrated individually to specification-valid progressive disclosure, and directly reviewed against every positive/negative trigger and output contract in its `evals/evals.json`. | `internal/skills/builtin/<name>/` with application client validation | success, failure, malformed input, boundary, platform, Unicode, migration, compatibility, security; cancellation/timeout/cleanup/concurrency apply when a bundled script or workflow invokes them | per-skill migration packet, `evals/evals.json`, automated structure validation, direct case-by-case agent review, sanitized human-review record, and user/developer docs | representative exact-build explicit/implicit activation and resource evidence; skill-specific external runtimes recorded only when actually available | verified |
| COMMAND-PARITY-01 | `/skills`, `/skill-audit`, `/skill-new`, `/init-re`, `/status-re`, `/doctor`, and `/help` each receive an evidence-backed equivalent, improved, intentionally changed, or not-applicable disposition; `/mcp` remains Stage H. | application-owned command registry and owning backend services | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, persistence, migration, compatibility, concurrency, security as applicable per command | direct Legacy traceability, focused command/service tests, architecture checks, and synchronized help/user docs | exact-build evidence for every retained Stage G command | verified |

## Acceptance evidence

- `COMMAND-01`: source and focused automated behavior verify the seven-command typed registry, registry-derived CLI/TUI help, completion order, retained unknown-command warning, typed intents, and presentation-only rendering. Exact-build runtime verified `/help`, `/models`, `/skills`, `/skill`, `/clear`, `/init-re`, `/status-re`, resize, and exit.
- `SKILLS-CLIENT-01`: discovery, validation, trust, precedence, disabled diagnostics, bounds, Unicode, link/non-regular refusal, replacement races, copy safety, and reload behavior are verified in `internal/skills`, configuration, app, and controller tests. Runtime verified bundled plus trusted-project discovery and path-free shadow diagnostics.
- `SKILLS-ACTIVATE-01`: explicit/model activation, deduplication, prompt disclosure, exact on-demand resources, reset, refreshed catalogs, constrained tool schemas, concurrency, secrecy, and dynamic context budgeting are verified by focused tests. Exact-build Devstral turns verified explicit activation, post-clear implicit activation, and exact resource loading.
- `COMMAND-PARITY-01`: `/init-re` and `/status-re` are improved secure equivalents and passed exact-build runtime; `/skill-audit` and `/skill-new` are intentionally replaced by standards validation/direct evaluation; `/doctor` is not applicable to Stage G in its Legacy form; `/mcp` remains Stage H.
- `SKILL-MIGRATION-01`: complete source-by-source disposition and automated structural validation are verified for all 28 Legacy skill files. Twenty-one bundled migrations each passed direct review of eight positive, eight negative, and three output-contract cases (336 trigger cases and 63 output contracts total); one workflow is merged, one stale link router is rejected, and five MCP-only guides are Stage H-deferred. The sanitized direct-review record is `E:\ReProject\StageG-Manual-Evaluation-Devstral\human-review.md`. No platform-specific external-tool execution is claimed where no lawful target/tool environment was available.
- Exact-build runtime on Devstral verified the full seven-command registry, retained Gemma profile, explicit and implicit activation, on-demand project-skill resource reading, evidence-workspace initialization/status, three terminal sizes, clean exit, and shell restoration. Visual review also found and drove a tested fix for an absolute-path shadow-diagnostic leak. Evidence is retained at `E:\ReProject\StageG-Runtime-Acceptance`.

- The canonical gate passed on Go 1.26.4 at exactly 3184/3184 statements. GitHub Actions run `29603832999` passed on both `windows-latest` and `ubuntu-latest` against the implementation commit.

Stage G has no blocked or unverified row. Source traceability, complete behavioral categories, meaningful docs, direct per-skill evaluations, exact-build runtime evidence, the canonical gate, and dual-platform CI agree.
