# Stage E Behavioral Surface Inventory

This accepted inventory audits Legacy agent/runtime behavior against the accepted current backend. It does not authorize tools, commands, skills, MCP, hosted-service budget behavior, or TUI changes.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| AGENT-03 | A user goal retains stable identity across model/tool rounds; calls run sequentially and remain paired; cancellation and stale work cannot leak or reissue side effects; bounded failure/runaway/empty/verification policies preserve the Legacy capability. Legacy parity over accepted Stage C ownership. | `internal/agent`, `internal/app/controller` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence and migration are not applicable because turn state is intentionally ephemeral | `TestTurnStateLifecycle`, `TestToolWorkAndSequentialPairing`, `TestToolCancellationAndStaleCleanup`, `TestToolFailureAndRunawayPolicy`, `TestCloseDecisions`, `TestRuntimeCancelTurnDropsOnlyRunningToolGoal`, runtime/controller suites, Stage C inventory, D-017, and Stage E packet | Exact build `b4fee3c` completed four ordered file tools, cancelled a running PowerShell tool without its result or side effect leaking/reissuing, recovered on the next turn, exited cleanly, and restored the shell | verified |
| CTX-02 | Packed requests stay within the declared budget while preserving valid recent conversation, complete tool groups, a real user task, Unicode-safe bounded tool output, and strict-backend-safe automated nudges. Legacy parity with current defensive improvements. | `internal/ctx`, `internal/agent` | success, failure, malformed input, boundary, Unicode, compatibility, concurrency, security; cancellation, timeout, cleanup, platform, persistence, and migration are not applicable to pure in-memory deterministic packing | Complete `internal/ctx` suite including reused-ID, trailing-nudge, empty-tail, partial-group, Unicode, reserve, and anchor cases; `TestBuildMessagesAndTools`; `TestRuntimeStartsPackedRound`; D-017; package/API docs; Stage E packet | Exact-build Gemma turns corroborated successful packed requests across a model reply, four-tool round, cancellation, and recovery; deterministic content correctness remains proven by the complete focused tests | verified |
| LLM-02 | The model boundary preserves streamed content/reasoning/tool fragments, valid final messages, transient retry, sticky reasoning fallback, activity-based idle timeout, cancellation, context probing, bounded errors, and secret-safe diagnostics required by the agent. Legacy parity with current injected/testable transport. | `internal/llm`, `internal/agent`, `internal/provider` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence and migration are not applicable because client fallback state is process-local | Complete `internal/llm` suite (`TestIdleTimeoutFromEnv` through `TestProbeMapsUnauthorized`), agent stream/diagnostic suites, provider tests, D-017, package/API docs, and Stage E packet | Exact build `b4fee3c` streamed with LM Studio `google/gemma-4-12b-qat`, cancelled an active stream, suppressed stale effects, recovered, and retained clean terminal closure; probe/error branches remain deterministically covered below presentation | verified |

The Legacy skills-template classifier is explicitly not an agent decision surface. Its `internal/classifier` source and `/skill classify` use remain Stage G evidence and are not represented as implemented here.

Stage E has no blocked or unverified row. Source inspection, focused and canonical automation, exact-build runtime reports, reviewed screenshots, documentation checks, and dual-platform CI agree.

## Audit dispositions

| Legacy contract | Disposition | Current evidence |
|---|---|---|
| One turn cancellation root, ordered calls, complete pairing, stale close/result rejection, final accounting | improved | `internal/agent` uses stable turn/round/work identities and opaque work rather than mutable TUI channel/context identity. Running-tool cancellation additionally removes the incomplete goal to prevent side-effect reissue. |
| Same-target failure, runaway, empty-reply, leaked-call, and verification policies | equivalent | Thresholds, queue-drain ordering, re-arm/latch/reset behavior, honest `unverified` suppression, and soft-nudge semantics match. Current branding and the already-accepted verification wording are non-compatibility text refinements. |
| Budgeted newest-first packing, head/tail truncation, tool pairing, task anchoring, system-nudge demotion | improved | Current positional pairing rejects reused-ID cross-turn false matches and recovers the newest complete tool group past trailing nudges or empty replies without resurrecting partial groups. |
| Reasoning/content/tool-fragment SSE, reasoning fallback, probe, cancellation, activity watchdog | improved | Current transport adds mid-stream error-frame detection and bounded transient pre-stream retries behind injected timing/HTTP boundaries. |
| Five-minute Legacy idle default | intentionally changed | Current accepted default is one hour, still activity-reset and environment-overridable, to avoid killing slow local inference during long prefill/generation. |
| Hosted budget/quota headers and 402 account behavior | not applicable | Intentionally removed and accepted in Stage A; generic authentication, status errors, and context-window probing remain. |
| Skill-template classifier | deferred | Its inputs, outputs, tests, and only user integration are skills and skill slash commands; Stage G owns the standards-based replacement decision. |
