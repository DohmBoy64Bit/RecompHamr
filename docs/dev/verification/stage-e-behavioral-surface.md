# Stage E Behavioral Surface Inventory

This active inventory audits Legacy agent/runtime behavior against the accepted current backend. It does not authorize tools, commands, skills, MCP, hosted-service budget behavior, or TUI changes.

| Surface ID | Contract and origin | Owner | Applicable categories | Tests and documentation | Runtime/manual evidence | Status |
|---|---|---|---|---|---|---|
| AGENT-03 | A user goal retains stable identity across model/tool rounds; calls run sequentially and remain paired; cancellation and stale work cannot leak or reissue side effects; bounded failure/runaway/empty/verification policies preserve the Legacy capability. Legacy parity over accepted Stage C ownership. | `internal/agent`, `internal/app/controller` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence and migration are not applicable because turn state is intentionally ephemeral | `TestTurnStateLifecycle`, `TestToolWorkAndSequentialPairing`, `TestToolCancellationAndStaleCleanup`, `TestToolFailureAndRunawayPolicy`, `TestCloseDecisions`, runtime/controller suites, Stage C inventory, and Stage E packet | Exact-build ordered-tool and stream/tool cancellation evidence must be mapped to the complete Stage E contract | unverified |
| CTX-02 | Packed requests stay within the declared budget while preserving valid recent conversation, complete tool groups, a real user task, Unicode-safe bounded tool output, and strict-backend-safe automated nudges. Legacy parity with current defensive improvements. | `internal/ctx`, `internal/agent` | success, failure, malformed input, boundary, Unicode, compatibility, concurrency, security; cancellation, timeout, cleanup, platform, persistence, and migration are not applicable to pure in-memory deterministic packing | Complete `internal/ctx` suite, `TestBuildMessagesAndTools`, `TestRuntimeStartsPackedRound`, package/API docs, and Stage E packet | Runtime request summaries may corroborate packing; content correctness is primarily deterministic test evidence | unverified |
| LLM-02 | The model boundary preserves streamed content/reasoning/tool fragments, valid final messages, transient retry, sticky reasoning fallback, activity-based idle timeout, cancellation, context probing, bounded errors, and secret-safe diagnostics required by the agent. Legacy parity with current injected/testable transport. | `internal/llm`, `internal/agent`, `internal/provider` | success, failure, malformed input, boundary, cancellation, timeout, cleanup, platform, Unicode, compatibility, concurrency, security; persistence and migration are not applicable because client fallback state is process-local | Complete `internal/llm` suite, agent stream/diagnostic suites, provider tests, package/API docs, and Stage E packet | Exact-build Gemma streaming, cancellation, recovery, and context/probe evidence required | unverified |

The Legacy skills-template classifier is explicitly not an agent decision surface. Its `internal/classifier` source and `/skill classify` use remain Stage G evidence and are not represented as implemented here.
