# `ghidra-mcp` Skill Stage H Disposition

Legacy `RecompHamr-Legacy-main/internal/skills/ghidra-mcp.md` was read completely. The mandatory current Agent Skills authority set and official Codex guidance had been read before this individual disposition.

**Intentionally deferred to Stage H; no Stage G skill is bundled.** The Legacy file is a capability guide for a Ghidra MCP server: `/mcp` setup and connection, a configurable remote tool surface, program/loader observation, analysis mutation, batch operations, and remote failure handling. Stage G excludes MCP servers, transports, configuration, and tools. A catalog entry now would expose instructions for capabilities RecompHamr does not own or provide.

The durable static-analysis principles—record target and address-space identity, distinguish raw bytes/disassembly from decompiler hypotheses, corroborate boundaries and names, preserve xrefs and provenance, and avoid unsupported renames—are already covered by general RE/evidence skills. Stage H must verify the then-current Ghidra integration, loader/tool capabilities, mutation/trust policy, size/time/cancellation behavior, and secret-safe diagnostics before authoring a new standards-valid skill and evaluations. Legacy product versions, installation commands, tool counts/names, and environment variables are not accepted as current facts.

No parser, trigger, output, runtime, or human evaluation is claimed for a `ghidra-mcp` skill because no such Stage G skill exists.
