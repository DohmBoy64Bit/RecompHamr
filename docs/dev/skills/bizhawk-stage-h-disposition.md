# `bizhawk` Skill Stage H Disposition

Legacy `RecompHamr-Legacy-main/internal/skills/bizhawk.md` was read completely. The mandatory current Agent Skills authority set and official Codex guidance had been read before this individual disposition.

**Intentionally deferred to Stage H; no Stage G skill is bundled.** The Legacy skill is an operational contract for `dmang-dev/mcp-bizhawk`: `/mcp` discovery and connection, a Lua/socket bridge, remote `bizhawk.*` tools, capability enumeration, memory mutation, input, frame advance, save states, and screenshots. Stage G explicitly excludes MCP transports, servers, configuration, and tools. Publishing the instructions without those typed capabilities would create false availability and encourage invented tool calls.

The reusable evidence principles—identify the ROM/core/version, enumerate actual memory domains and capabilities, record frame/address/width/endianness, checkpoint before mutation, and treat emulator observations as bounded evidence—are already represented where appropriate in migrated platform skills. Stage H must reread the current BizHawk bridge/server documentation and source, establish trust and mutation policy, test cancellation/output/path/security behavior, and create fresh trigger/output evaluations before approving any future `bizhawk` skill. Legacy install commands, fixed tool counts, ports, memory-domain tables, and error mappings are not accepted as current facts.

No parser, trigger, output, runtime, or human evaluation is claimed for a `bizhawk` skill because no such Stage G skill exists.
