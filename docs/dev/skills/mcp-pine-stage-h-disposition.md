# `mcp-pine` Skill Stage H Disposition

Legacy `RecompHamr-Legacy-main/internal/skills/mcp-pine.md` was read completely. The mandatory current Agent Skills authority set and official Codex guidance had been read before this individual disposition.

**Intentionally deferred to Stage H; no Stage G skill is bundled.** The Legacy file exclusively describes an RPCS3 PINE IPC MCP server: `/mcp` lifecycle, localhost transport, remote guest-memory/register mutation, breakpoints, stepping, and save-state operations. Those capabilities and their security boundary do not exist in Stage G.

The reusable principles—identify title/build/address space, pause for coherent snapshots, checkpoint before mutation, compare the same state, and treat emulator behavior as bounded reference evidence—may inform `ps3recomp` without advertising PINE tools. Stage H must verify the current server and RPCS3 contracts, local binding/authentication, tool schemas, mutation authorization, cancellation/time/output bounds, save-state cleanup, and secret-safe diagnostics before authoring and evaluating a replacement skill. Legacy install commands, tool names, environment variables, and failure mappings are not accepted as current facts.

No parser, trigger, output, runtime, or human evaluation is claimed for an `mcp-pine` skill because no such Stage G skill exists.
