# `sega2asm` Skill Merge Disposition

Legacy `RecompHamr-Legacy-main/internal/skills/sega2asm.md` was read completely. The mandatory current Agent Skills authority set and official Codex guidance had been read before this individual disposition.

**Intentionally merged into `gen-decomp`; no separate Stage G skill is bundled.** The reusable user intent—Genesis/Mega Drive split planning, configuration-owned generated output, M68000/Z80 segmentation, compression classification, symbols/charmaps, VDP evidence, assets, and runtime corroboration—is the same coherent workflow already migrated in `internal/skills/builtin/gen-decomp/SKILL.md`. A second catalog entry would create overlapping activation and duplicate instructions without progressive-disclosure value.

The content left after that merge is an MCP server wrapper: `/mcp` lifecycle, remote `sega2asm.*` tools, installation/environment setup, and fixed format/game/tool-count claims. MCP belongs to Stage H. If a future typed adapter is approved, Stage H must verify the current project source, CLI/server schemas, cancellation/output/path/mutation behavior, and tool/version facts before authoring its capability guidance. Legacy command lines, counts, install-at-latest instructions, and detector certainty are not accepted as current facts.

The `gen-decomp` eval set covers tool-neutral splitting and compression behavior. No separate parser, trigger, output, runtime, or human evaluation is claimed for a `sega2asm` skill because no separate Stage G skill exists.
