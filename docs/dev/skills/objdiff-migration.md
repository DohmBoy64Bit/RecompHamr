# `objdiff` Skill Migration Record

Legacy `RecompHamr-Legacy-main/internal/skills/objdiff.md` was read completely; its tool/configuration references were project-dependent and no separate Legacy resource file was supplied. The mandatory current Agent Skills authority set and official Codex guidance had been read before this individual edit.

Trigger for projects explicitly using encounter/objdiff-style object comparison: target/base provenance, configuration and build identity, unit comparison, mismatch classification, compiler experiments, and honest progress reporting. Exclude runtime parity, source-only review, and projects governed by another matcher.

**Improved.** The migration preserves objective comparison, fresh builds, compiler/flag analysis, per-unit evidence, stale-input warnings, and progress caveats. It removes MCP setup/tool calls, automatic installation, hard-coded schemas/statuses/architecture support, and the universal claim that objdiff is every project's final proof gate. It adds relocation/normalization identity, inconclusive state, explicit missing/failed denominators, proprietary-data minimization, and complete-surface versus unit-level proof boundaries. No script is bundled because the project's pinned matcher and build integration own the executable contract.

Final skill: `internal/skills/builtin/objdiff/SKILL.md`. Its eval set has eight positive, eight negative, and three provenance/reporting cases. Automated parser/client checks apply. Direct agent review on 2026-07-17 passed 8/8 positive, 8/8 negative, and 3/3 output contracts. No project-pinned objdiff target/base pair was available, so no object comparison is claimed.
